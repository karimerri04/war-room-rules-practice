import 'package:flutter/material.dart';

import 'app/war_room_app.dart';

/// Entry point of the Flutter frontend.
///
/// The app itself is declared in [WarRoomApp] so that the bootstrap file stays
/// minimal and the application configuration remains testable and isolated.
void main() {
  runApp(const WarRoomApp());
}